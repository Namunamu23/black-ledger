# Superseded — see `AUDIT_PROMPT.md`

This file has been merged into `AUDIT_PROMPT.md`. That file is now the single canonical god-mode audit prompt. Paste from `AUDIT_PROMPT.md` only. This stub is kept because the session cannot delete files directly; safe to `git rm` whenever you next clean up.
